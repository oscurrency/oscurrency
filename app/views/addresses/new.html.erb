  <h2><%= t('addresses.new.header') %></h2>

  <%= bootstrap_form_for [current_person, @address] do |form| %>
    <%= render :partial => 'form', :object => form %>
    <%= form.primary t('button_create')%>
  <% end %>

